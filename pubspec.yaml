name: app
description: "A new Flutter project."
publish_to: 'https://github.com/ultra-rony/kpi-drive-app'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  get_it: ^9.2.0
  flutter_bloc: ^9.1.1
  logger: ^2.6.2
  freezed_annotation: ^3.1.0
  shared_preferences: ^2.5.4
  flutter_dotenv: ^6.0.0
  json_annotation: ^4.9.0
  injectable: ^2.7.1+4
  auto_route: ^11.1.0
  google_fonts: ^7.0.2
  intl: ^0.20.2
  appflowy_board: ^0.1.2
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.10.5
  injectable_generator: ^2.12.0
  auto_route_generator: ^10.4.0
  json_serializable: ^6.11.4
  freezed: ^3.2.4

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env
